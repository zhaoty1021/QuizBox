# 微信小程序设计文档：趣味测试合集

**日期**：2026-04-03
**类型**：微信小程序（原生）
**定位**：休闲娱乐 + 广告变现
**目标**：本文档作为完整开发规格，可直接用于 AI 代码生成，无需额外沟通。

### 项目命名

| 中文名 | 英文名 | 风格 |
|--------|--------|------|
| 小测盒 | QuizBox | 简洁，好记，盲盒感 |
| 测一测研究所 | QuizLab | 趣味 + 伪正经，搜索友好 |
| 灵魂快照 | SoulSnap | 文艺神秘，契合深色 UI |
| 脑洞测试局 | MindQuiz | 轻松沙雕向 |
| 测了个测 | QuizPop | 洗脑口语化 |

> 英文名用于项目目录和代码仓库命名，中文名用于小程序对外展示。建议优先选择**「小测盒 / QuizBox」**或**「测一测研究所 / QuizLab」**，名称中带"测"利于微信搜索关键词命中。

---

## 1. 产品概述

一款以"测试 + 互动叙事"为核心的微信小程序，提供多套趣味测试（命运玄学、心理深挖、人生模拟、沙雕整活），用户答题后看激励视频解锁完整结果，并通过分享卡片自然传播。

**核心价值主张**：免费玩、结果有趣、分享有面子。
**变现模式**：激励视频广告（主）+ Banner 广告（辅）。
**传播机制**：Canvas 生成个性化结果海报 → 保存相册 → 朋友圈/微信群扩散。

---

## 1.1 场景可行性分析

### 优势

1. **需求真实**：趣味测试在微信生态里有持续传播力，"前世身份"、"心理年龄"这类话题自带社交货币属性，用户愿意做、愿意晒。
2. **变现路径清晰**：激励视频是微信小程序生态中 eCPM 最高的广告形式之一，设计了两次激励视频（解锁主结果 + 解锁隐藏内容），单次用户可产出 2 次广告曝光。
3. **传播闭环完整**：Canvas 生成海报 → 保存相册 → 朋友圈扩散 → 新用户扫码进入，是经过验证的裂变路径。
4. **开发成本极低**：纯前端，无后端，硬编码数据，三个页面，技术栈简单。
5. **内容可扩展**：每套测试就是一个 JS 文件，可以持续用 AI 生成新内容，运营成本接近零。

### 风险与不足

1. **审核风险**：微信对"测试类"小程序审核偏严，尤其涉及"命运"、"前世"、"玄学"相关内容，可能被以"封建迷信"为由拒审。建议弱化玄学措辞，用"趣味"、"娱乐"定位包装。
2. **留存问题**：纯测试类产品用户留存天然偏低，做完几套测试后缺乏回访理由。v2 提到的"每日一测"和"签到机制"是必要的，但 v1 没有。
3. **海报分享限制**：微信不支持小程序直接调起朋友圈，只能"保存到相册"让用户手动发，转化有一定损失。
4. **广告门槛**：需要 1000 UV 才能开通流量主，冷启动阶段没有收入。
5. **内容同质化**：市面上类似产品很多，需要在内容质量（题目设计、结果文案）上做出差异化。

---

## 1.2 为什么是小程序而非小游戏

| 维度 | 小程序 | 小游戏 |
|------|--------|--------|
| **技术栈** | WXML/WXSS/JS（本方案） | Canvas/WebGL 渲染引擎 |
| **适用场景** | 表单交互、信息展示、答题 | 需要实时渲染的游戏画面 |
| **审核类目** | 娱乐 > 其他娱乐 | 游戏类目（需版号，门槛极高） |
| **广告能力** | Banner + 激励视频 | 同左，但需游戏类目资质 |

**结论：本项目应做微信小程序。** 理由：
- 交互形式是答题选择 + 结果展示，本质是表单和页面跳转，不需要逐帧渲染的游戏画面
- 小游戏需要游戏版号，审批流程长、成本高，对轻量测试类产品没有必要
- 小程序的分享能力（`onShareAppMessage`）和广告组件（`<ad>`、激励视频）已满足所有功能需求
- 唯一用到 Canvas 的地方是生成海报图片，小程序的 Canvas 2D API 完全胜任

---

## 2. 用户旅程

```
打开小程序
    ↓
首页 Feed（浏览测试卡片，底部 Banner 广告）
    ↓
点击某个测试 → 答题页（8~12 题，全程无广告）
    ↓
答题完成 → 结果解锁页（模糊遮罩 + 激励视频按钮）
    ↓
观看激励视频（完播）→ 解锁完整结果
    ↓
结果页（查看结果 + 分享 + 解锁隐藏内容 + 推荐其他测试，底部 Banner）
    ↓
分享卡片（Canvas 生成海报 → 保存相册 / 转发给朋友）
```

---

## 3. 视觉风格

整体采用**深色系神秘风格**，契合命运/心理测试氛围。

### 3.1 配色方案

```css
/* 主色 */
--color-bg:         #0d0d1a;   /* 页面背景，深紫黑 */
--color-card:       #16213e;   /* 卡片背景 */
--color-card-hover: #1a2a50;   /* 卡片按下态 */
--color-primary:    #e94560;   /* 主色调，玫红 */
--color-secondary:  #533483;   /* 辅助紫 */
--color-accent:     #4a9eff;   /* 强调蓝 */

/* 文字 */
--color-text-primary:   #ffffff;
--color-text-secondary: #aaaaaa;
--color-text-muted:     #666666;

/* 分类标签色 */
--color-destiny:    #e94560;   /* 命运 */
--color-psychology: #533483;   /* 性格 */
--color-life:       #0f8a8a;   /* 人生 */
--color-comedy:     #e8a838;   /* 沙雕 */
```

### 3.2 字体规范

```css
/* 标题 */
font-size: 36rpx; font-weight: bold;
/* 副标题 / 题目 */
font-size: 30rpx; font-weight: normal;
/* 正文 */
font-size: 26rpx; line-height: 1.8;
/* 小字 / 标签 */
font-size: 22rpx;
/* 全局字体 */
font-family: -apple-system, "PingFang SC", sans-serif;
```

### 3.3 圆角 & 间距

```css
--radius-card:   16rpx;
--radius-button: 50rpx;   /* 胶囊按钮 */
--radius-tag:    20rpx;
--spacing-page:  30rpx;   /* 页面左右边距 */
--spacing-card:  24rpx;   /* 卡片内边距 */
```

### 3.4 分类对应渐变色（海报用）

| 分类 | 渐变起止色 |
|------|-----------|
| destiny（命运） | `#1a0533` → `#4a0e5e` |
| psychology（性格） | `#0d1b3e` → `#1a3a6e` |
| life（人生） | `#0a2a2a` → `#1a5a5a` |
| comedy（沙雕） | `#2a1a00` → `#5a3a00` |

---

## 4. 全局配置

### 4.1 `app.json`

```json
{
  "pages": [
    "pages/index/index",
    "pages/quiz/quiz",
    "pages/result/result"
  ],
  "window": {
    "backgroundTextStyle": "light",
    "navigationBarBackgroundColor": "#0d0d1a",
    "navigationBarTitleText": "趣味测试",
    "navigationBarTextStyle": "white",
    "backgroundColor": "#0d0d1a"
  },
  "style": "v2",
  "sitemapLocation": "sitemap.json"
}
```

### 4.2 `app.js`

```js
App({
  globalData: {
    // 广告单元 ID（开发阶段用测试 ID，上线后替换真实 ID）
    rewardedVideoAdId: 'adunit-xxxxxxxxxxxxxxxx',
    bannerAdId: 'adunit-yyyyyyyyyyyyyyyy',
  }
})
```

---

## 5. 页面设计与实现规格

### 5.1 首页 Feed（`pages/index/index`）

#### 功能
- 顶部分类 Tab 筛选测试列表
- 测试卡片瀑布流
- 底部固定 Banner 广告

#### `index.js` data 结构

```js
data: {
  categories: [
    { key: 'all',        label: '全部' },
    { key: 'destiny',    label: '命运' },
    { key: 'psychology', label: '性格' },
    { key: 'life',       label: '人生' },
    { key: 'comedy',     label: '沙雕' },
  ],
  activeCategory: 'all',
  tests: [],          // 当前展示的测试列表（根据分类过滤）
  allTests: [],       // 全量测试列表（从 data/tests.js 加载）
}
```

#### 核心逻辑

```js
// onLoad：加载所有测试
onLoad() {
  const { TESTS } = require('../../data/tests')
  this.setData({ allTests: TESTS, tests: TESTS })
},

// 切换分类
onCategoryTap(e) {
  const key = e.currentTarget.dataset.key
  const filtered = key === 'all'
    ? this.data.allTests
    : this.data.allTests.filter(t => t.category === key)
  this.setData({ activeCategory: key, tests: filtered })
},

// 点击卡片进入测试
onTestTap(e) {
  const { id } = e.currentTarget.dataset
  wx.navigateTo({ url: `/pages/quiz/quiz?testId=${id}` })
},
```

#### WXML 结构

```xml
<view class="page">
  <!-- 分类 Tab -->
  <scroll-view class="tab-bar" scroll-x>
    <view
      wx:for="{{categories}}" wx:key="key"
      class="tab-item {{activeCategory === item.key ? 'active' : ''}}"
      bindtap="onCategoryTap" data-key="{{item.key}}"
    >{{item.label}}</view>
  </scroll-view>

  <!-- 测试卡片列表 -->
  <scroll-view class="test-list" scroll-y>
    <view
      wx:for="{{tests}}" wx:key="id"
      class="test-card"
      bindtap="onTestTap" data-id="{{item.id}}"
    >
      <image class="test-cover" src="{{item.cover}}" mode="aspectFill"/>
      <view class="test-info">
        <view class="test-tag tag-{{item.category}}">{{item.categoryLabel}}</view>
        <text class="test-title">{{item.title}}</text>
        <text class="test-count">{{item.count}}人已测</text>
      </view>
    </view>
  </scroll-view>

  <!-- Banner 广告（底部固定） -->
  <view class="banner-wrap">
    <ad unit-id="{{bannerAdId}}" ad-type="banner"></ad>
  </view>
</view>
```

---

### 5.2 答题页（`pages/quiz/quiz`）

#### 功能
- 接收 `testId` 参数，加载对应测试
- 逐题展示，点选后自动跳下一题
- 全程无广告
- 最后一题后计算结果，跳转结果页

#### `quiz.js` data 结构

```js
data: {
  test: null,           // 当前测试完整数据
  currentIndex: 0,      // 当前题目索引（0-based）
  answers: [],          // 用户答案数组，存选项索引
  selectedOption: -1,   // 当前题已选选项（用于高亮），-1 表示未选
  progress: 0,          // 进度百分比 0~100
}
```

#### 核心逻辑

```js
onLoad(options) {
  const { testId } = options
  const testModule = require(`../../data/tests/${testId}`)
  const test = testModule.default
  this.setData({ test, progress: 0 })
},

onOptionTap(e) {
  const optionIndex = e.currentTarget.dataset.index
  const { test, currentIndex, answers } = this.data
  const newAnswers = [...answers, optionIndex]

  // 高亮选中项，延迟 300ms 后跳下一题
  this.setData({ selectedOption: optionIndex })
  setTimeout(() => {
    if (currentIndex + 1 >= test.questions.length) {
      // 最后一题：计算结果，跳转结果页
      const { calcResult } = require('../../utils/scoring')
      const resultKey = calcResult(test.questions, newAnswers)
      wx.redirectTo({
        url: `/pages/result/result?testId=${test.id}&resultKey=${resultKey}`
      })
    } else {
      const next = currentIndex + 1
      const progress = Math.round((next / test.questions.length) * 100)
      this.setData({
        currentIndex: next,
        answers: newAnswers,
        selectedOption: -1,
        progress,
      })
    }
  }, 300),
},
```

#### WXML 结构

```xml
<view class="page">
  <!-- 进度条 -->
  <view class="progress-bar">
    <view class="progress-fill" style="width: {{progress}}%"></view>
  </view>
  <text class="progress-text">{{currentIndex + 1}} / {{test.questions.length}}</text>

  <!-- 题目 -->
  <view class="question-wrap">
    <text class="question-text">
      {{test.questions[currentIndex].text}}
    </text>
  </view>

  <!-- 选项列表 -->
  <view class="options-wrap">
    <view
      wx:for="{{test.questions[currentIndex].options}}"
      wx:key="index"
      class="option-item {{selectedOption === index ? 'selected' : ''}}"
      bindtap="onOptionTap"
      data-index="{{index}}"
    >
      <text class="option-label">{{['A','B','C','D'][index]}}</text>
      <text class="option-text">{{item.text}}</text>
    </view>
  </view>
</view>
```

---

### 5.3 结果页（`pages/result/result`）

结果页有两个状态，用 `unlocked` 布尔值控制，**同一个页面文件**，不跳转。

#### `result.js` data 结构

```js
data: {
  test: null,          // 测试基本信息
  result: null,        // 当前结果对象 { title, desc, hidden }
  unlocked: false,     // 是否已解锁完整结果
  hiddenUnlocked: false, // 是否已解锁隐藏内容
  posterPath: '',      // 生成的海报临时路径
  generating: false,   // 是否正在生成海报
  rewardedAd: null,    // 激励视频广告实例
}
```

#### 核心逻辑

```js
onLoad(options) {
  const { testId, resultKey } = options
  const testModule = require(`../../data/tests/${testId}`)
  const test = testModule.default
  const result = test.results[resultKey]

  // 初始化激励视频广告
  const app = getApp()
  const rewardedAd = wx.createRewardedVideoAd({
    adUnitId: app.globalData.rewardedVideoAdId
  })
  rewardedAd.load()

  this.setData({ test, result, rewardedAd })
  this.data.rewardedAd = rewardedAd  // 不触发 setData，直接赋值
},

// 解锁主结果（第一个激励视频）
onUnlockTap() {
  this.data.rewardedAd.show().catch(() => {
    // 广告未加载好，重试
    this.data.rewardedAd.load().then(() => this.data.rewardedAd.show())
  })
  this.data.rewardedAd.onClose(res => {
    if (res && res.isEnded) {
      this.setData({ unlocked: true })
    } else {
      wx.showToast({ title: '需要看完才能解锁哦', icon: 'none' })
    }
  })
},

// 解锁隐藏内容（第二个激励视频，复用同一实例）
onUnlockHiddenTap() {
  this.data.rewardedAd.load().then(() => {
    this.data.rewardedAd.show()
    this.data.rewardedAd.onClose(res => {
      if (res && res.isEnded) {
        this.setData({ hiddenUnlocked: true })
      } else {
        wx.showToast({ title: '需要看完才能解锁哦', icon: 'none' })
      }
    })
  })
},

// 生成海报
onGeneratePoster() {
  this.setData({ generating: true })
  const { generatePoster } = require('../../utils/poster')
  generatePoster({
    category: this.data.test.category,
    testTitle: this.data.test.title,
    resultTitle: this.data.result.title,
    resultDesc: this.data.result.desc,
  }).then(path => {
    this.setData({ posterPath: path, generating: false })
  })
},

// 保存海报到相册
onSavePoster() {
  wx.saveImageToPhotosAlbum({
    filePath: this.data.posterPath,
    success: () => wx.showToast({ title: '已保存到相册' }),
    fail: () => wx.showToast({ title: '请先授权访问相册', icon: 'none' }),
  })
},

// 转发配置
onShareAppMessage() {
  return {
    title: `我测出了「${this.data.result.title}」，你来测测？`,
    path: '/pages/index/index',
    imageUrl: this.data.test.cover,
  }
},
```

#### WXML 结构

```xml
<view class="page">

  <!-- 解锁前：模糊遮罩状态 -->
  <block wx:if="{{!unlocked}}">
    <view class="lock-screen">
      <view class="blur-result">
        <text class="result-title-blur">✨ 你的专属结果已生成</text>
        <view class="blur-mask"></view>
      </view>
      <text class="lock-hint">完成测试，解锁你的专属结果</text>
      <button class="btn-unlock" bindtap="onUnlockTap">
        🎁 观看视频 · 免费解锁
      </button>
    </view>
  </block>

  <!-- 解锁后：结果展示 -->
  <block wx:else>
    <view class="result-screen">
      <!-- 结果标题 -->
      <text class="result-title">{{result.title}}</text>
      <text class="result-desc">{{result.desc}}</text>

      <!-- 隐藏内容区 -->
      <view class="hidden-section">
        <block wx:if="{{!hiddenUnlocked}}">
          <view class="hidden-blur">
            <text>隐藏揭秘：更深层的你...</text>
            <view class="blur-mask-sm"></view>
          </view>
          <button class="btn-unlock-hidden" bindtap="onUnlockHiddenTap">
            🔮 解锁隐藏揭秘
          </button>
        </block>
        <block wx:else>
          <text class="hidden-text">{{result.hidden}}</text>
        </block>
      </view>

      <!-- 操作按钮 -->
      <view class="action-btns">
        <button class="btn-primary" bindtap="onGeneratePoster" loading="{{generating}}">
          🖼️ 生成专属卡片
        </button>
        <button class="btn-secondary" open-type="share">
          📤 分享给朋友
        </button>
        <navigator url="/pages/index/index" open-type="switchTab">
          再测一套
        </navigator>
      </view>

      <!-- 海报预览（生成后显示） -->
      <view wx:if="{{posterPath}}" class="poster-preview">
        <image src="{{posterPath}}" mode="widthFix" class="poster-img"/>
        <button class="btn-save" bindtap="onSavePoster">保存到相册</button>
      </view>

      <!-- 推荐其他测试 -->
      <view class="recommend-section">
        <text class="section-title">猜你也想测</text>
        <scroll-view class="recommend-list" scroll-x>
          <!-- 渲染其他测试卡片，点击跳转答题页 -->
        </scroll-view>
      </view>

      <!-- Banner 广告 -->
      <view class="banner-wrap">
        <ad unit-id="{{bannerAdId}}" ad-type="banner"></ad>
      </view>
    </view>
  </block>

</view>
```

---

## 6. 工具模块完整规格

### 6.1 `utils/scoring.js`

```js
/**
 * 计算测试结果
 * @param {Array} questions - 题目数组
 * @param {Array} answers   - 用户答案索引数组（与 questions 等长）
 * @returns {string}        - 得分最高的结果 key；平局取先出现的
 */
export function calcResult(questions, answers) {
  const scores = {}

  questions.forEach((q, i) => {
    const option = q.options[answers[i]]
    if (!option) return  // 防御性检查
    Object.entries(option.scores).forEach(([key, val]) => {
      scores[key] = (scores[key] || 0) + val
    })
  })

  const sorted = Object.entries(scores).sort((a, b) => b[1] - a[1])
  return sorted[0][0]
}
```

### 6.2 `utils/ad.js`

```js
/**
 * 激励视频广告封装
 * 用法：
 *   import { showRewardedAd } from '../../utils/ad'
 *   showRewardedAd(adUnitId, onSuccess, onFail)
 */
export function showRewardedAd(adUnitId, onSuccess, onFail) {
  const ad = wx.createRewardedVideoAd({ adUnitId })

  ad.onError(err => {
    console.error('广告加载失败', err)
    wx.showToast({ title: '广告加载失败，请稍后重试', icon: 'none' })
    onFail && onFail(err)
  })

  ad.onClose(res => {
    if (res && res.isEnded) {
      onSuccess && onSuccess()
    } else {
      wx.showToast({ title: '需要看完才能解锁哦 ~', icon: 'none' })
      onFail && onFail('not_ended')
    }
  })

  ad.load()
    .then(() => ad.show())
    .catch(err => {
      // 已加载直接 show
      ad.show().catch(() => {
        wx.showToast({ title: '广告暂时不可用', icon: 'none' })
        onFail && onFail(err)
      })
    })
}
```

### 6.3 `utils/poster.js`

Canvas 2D 绘制分享海报，尺寸 750×1334rpx（以 375px 物理像素计算，即 canvas width=375, height=667）。

```js
/**
 * 生成分享海报
 * @param {object} params
 *   - category:    string  测试分类（决定背景色）
 *   - testTitle:   string  测试标题（顶部小字）
 *   - resultTitle: string  结果标题（主视觉大字）
 *   - resultDesc:  string  结果描述（中部正文）
 * @returns {Promise<string>} 临时文件路径
 */
export function generatePoster({ category, testTitle, resultTitle, resultDesc }) {
  return new Promise((resolve, reject) => {
    const query = wx.createSelectorQuery()
    query.select('#poster-canvas')
      .fields({ node: true, size: true })
      .exec(res => {
        const canvas = res[0].node
        const ctx = canvas.getContext('2d')
        const W = 375, H = 667  // px，对应 750×1334 rpx

        // 1. 背景渐变
        const GRADIENTS = {
          destiny:    ['#1a0533', '#4a0e5e'],
          psychology: ['#0d1b3e', '#1a3a6e'],
          life:       ['#0a2a2a', '#1a5a5a'],
          comedy:     ['#2a1a00', '#5a3a00'],
        }
        const [c1, c2] = GRADIENTS[category] || GRADIENTS.destiny
        const grad = ctx.createLinearGradient(0, 0, 0, H)
        grad.addColorStop(0, c1)
        grad.addColorStop(1, c2)
        ctx.fillStyle = grad
        ctx.fillRect(0, 0, W, H)

        // 2. 测试标题（顶部小字）
        ctx.fillStyle = 'rgba(255,255,255,0.6)'
        ctx.font = '14px PingFang SC'
        ctx.textAlign = 'center'
        ctx.fillText(testTitle, W / 2, 60)

        // 3. 装饰线
        ctx.strokeStyle = 'rgba(255,255,255,0.3)'
        ctx.lineWidth = 1
        ctx.beginPath()
        ctx.moveTo(40, 80); ctx.lineTo(W - 40, 80)
        ctx.stroke()

        // 4. 结果标题（大字，主视觉）
        ctx.fillStyle = '#ffffff'
        ctx.font = 'bold 28px PingFang SC'
        ctx.textAlign = 'center'
        wrapText(ctx, resultTitle, W / 2, 160, W - 80, 40)

        // 5. 描述文字（换行处理）
        ctx.fillStyle = 'rgba(255,255,255,0.85)'
        ctx.font = '16px PingFang SC'
        wrapText(ctx, resultDesc, W / 2, 260, W - 80, 26)

        // 6. 底部小程序码区域
        ctx.fillStyle = 'rgba(255,255,255,0.1)'
        ctx.roundRect(W/2 - 80, H - 160, 160, 120, 12)
        ctx.fill()

        // 7. 绘制小程序码图片
        const qrImg = canvas.createImage()
        qrImg.onload = () => {
          ctx.drawImage(qrImg, W/2 - 45, H - 150, 90, 90)
          ctx.fillStyle = 'rgba(255,255,255,0.6)'
          ctx.font = '12px PingFang SC'
          ctx.textAlign = 'center'
          ctx.fillText('长按扫码测一测', W / 2, H - 45)

          // 导出图片
          wx.canvasToTempFilePath({
            canvas,
            success: r => resolve(r.tempFilePath),
            fail: reject,
          })
        }
        qrImg.src = '/images/qrcode.png'
      })
  })
}

// 文字自动换行工具
function wrapText(ctx, text, x, y, maxWidth, lineHeight) {
  const chars = text.split('')
  let line = ''
  let currentY = y
  for (let i = 0; i < chars.length; i++) {
    const testLine = line + chars[i]
    if (ctx.measureText(testLine).width > maxWidth && i > 0) {
      ctx.fillText(line, x, currentY)
      line = chars[i]
      currentY += lineHeight
    } else {
      line = testLine
    }
  }
  ctx.fillText(line, x, currentY)
}
```

**注意**：结果页 WXML 需加隐藏 canvas 供 poster.js 使用：
```xml
<canvas type="2d" id="poster-canvas" style="width:375px;height:667px;position:fixed;left:-9999px;top:0;"></canvas>
```

---

## 7. 完整测试数据示例

以下是一套完整可用的测试数据，供参考格式和内容风格：

### `data/tests/past-life.js`

```js
export default {
  id: 'past-life',
  title: '你的前世是什么身份？',
  category: 'destiny',
  questions: [
    {
      text: '你最享受哪种环境？',
      options: [
        { text: '宁静的深山古寺', scores: { monk: 3, warrior: 0, scholar: 1, noble: 0 } },
        { text: '喧嚣的城市战场', scores: { monk: 0, warrior: 3, scholar: 0, noble: 1 } },
        { text: '书香弥漫的书院', scores: { monk: 1, warrior: 0, scholar: 3, noble: 1 } },
        { text: '金碧辉煌的宫殿', scores: { monk: 0, warrior: 1, scholar: 1, noble: 3 } },
      ]
    },
    {
      text: '遇到强大的对手，你第一反应是？',
      options: [
        { text: '冷静观察，寻找破绽', scores: { monk: 2, warrior: 1, scholar: 2, noble: 0 } },
        { text: '迎头而上，正面硬刚', scores: { monk: 0, warrior: 3, scholar: 0, noble: 1 } },
        { text: '以智谋化解，不战而胜', scores: { monk: 1, warrior: 0, scholar: 3, noble: 1 } },
        { text: '调动资源，借力打力', scores: { monk: 0, warrior: 1, scholar: 1, noble: 3 } },
      ]
    },
    {
      text: '你最看重人生中的什么？',
      options: [
        { text: '内心的平静与超脱', scores: { monk: 3, warrior: 0, scholar: 1, noble: 0 } },
        { text: '荣耀与被人铭记', scores: { monk: 0, warrior: 2, scholar: 0, noble: 2 } },
        { text: '知识与真理的探寻', scores: { monk: 1, warrior: 0, scholar: 3, noble: 0 } },
        { text: '权力与对他人的影响力', scores: { monk: 0, warrior: 1, scholar: 0, noble: 3 } },
      ]
    },
    {
      text: '如果你有一个秘密，你会？',
      options: [
        { text: '永远藏在心底，独自承受', scores: { monk: 3, warrior: 1, scholar: 1, noble: 0 } },
        { text: '告诉最信任的生死兄弟', scores: { monk: 0, warrior: 3, scholar: 0, noble: 1 } },
        { text: '写进日记，留给后人', scores: { monk: 1, warrior: 0, scholar: 3, noble: 1 } },
        { text: '视情况而定，秘密也是筹码', scores: { monk: 0, warrior: 0, scholar: 1, noble: 3 } },
      ]
    },
    {
      text: '你最恐惧的事情是？',
      options: [
        { text: '被欲望所控制，迷失自我', scores: { monk: 3, warrior: 0, scholar: 1, noble: 0 } },
        { text: '在战场上临阵脱逃', scores: { monk: 0, warrior: 3, scholar: 0, noble: 1 } },
        { text: '学问半途而废，一无所成', scores: { monk: 1, warrior: 0, scholar: 3, noble: 0 } },
        { text: '权力旁落，被人背叛', scores: { monk: 0, warrior: 1, scholar: 0, noble: 3 } },
      ]
    },
    {
      text: '别人眼中的你是？',
      options: [
        { text: '神秘莫测，难以捉摸', scores: { monk: 2, warrior: 0, scholar: 2, noble: 1 } },
        { text: '热血直爽，冲劲十足', scores: { monk: 0, warrior: 3, scholar: 0, noble: 0 } },
        { text: '博学多才，言辞犀利', scores: { monk: 0, warrior: 0, scholar: 3, noble: 1 } },
        { text: '优雅从容，气场十足', scores: { monk: 1, warrior: 0, scholar: 0, noble: 3 } },
      ]
    },
    {
      text: '临死前，你最希望留下的是什么？',
      options: [
        { text: '一本点化众生的经文', scores: { monk: 3, warrior: 0, scholar: 2, noble: 0 } },
        { text: '一段令人传唱的战歌', scores: { monk: 0, warrior: 3, scholar: 0, noble: 1 } },
        { text: '一部改变时代的著作', scores: { monk: 0, warrior: 0, scholar: 3, noble: 1 } },
        { text: '一个繁荣昌盛的王朝', scores: { monk: 0, warrior: 1, scholar: 0, noble: 3 } },
      ]
    },
    {
      text: '你对"命运"的态度是？',
      options: [
        { text: '命运由心，万法皆空', scores: { monk: 3, warrior: 0, scholar: 1, noble: 0 } },
        { text: '命运？我来创造命运', scores: { monk: 0, warrior: 3, scholar: 0, noble: 1 } },
        { text: '用知识读懂命运规律', scores: { monk: 1, warrior: 0, scholar: 3, noble: 0 } },
        { text: '命运是博弈，我要赢', scores: { monk: 0, warrior: 1, scholar: 0, noble: 3 } },
      ]
    },
  ],
  results: {
    monk: {
      title: '✨ 前世得道高僧',
      desc: '你前世超脱红尘，智慧深远，心怀慈悲却看透世情。历经千年修行，你懂得万物皆空，却也深知人间情义之重。今生的你内心平静，有独到见解，偶尔会觉得与这个世界格格不入——那是因为你的灵魂见过更广阔的天地。',
      hidden: '【隐藏揭秘】前世的你曾在圆寂前三天，悄悄下山，为一个素不相识的人了结了一段未了的缘。那个秘密，连你的传承弟子都不知道……',
    },
    warrior: {
      title: '⚔️ 前世沙场猛将',
      desc: '你前世征战四方，铁血柔情并存。百战沙场，你见过生死，也护过无数人的家园。那种燃烧一切的热血，在今生依然流淌——你行动力强，有领导气质，但内心深处有一块柔软之地，只为真正懂你的人而开。',
      hidden: '【隐藏揭秘】前世最后一战，你本可以撤退，但你选择了留下。那场战役改变了一个王朝的命运，而你的名字……被故意从史书中抹去了。',
    },
    scholar: {
      title: '📜 前世博学鸿儒',
      desc: '你前世笔走龙蛇，著书立说，以文字影响了无数人的命运。你相信知识的力量胜过刀剑，用思想照亮了一个时代。今生的你求知欲旺盛，敏感细腻，有时会因为想得太多而显得犹豫——那是智者的重量。',
      hidden: '【隐藏揭秘】前世的你曾写过一部被朝廷列为禁书的著作，书中藏着一个惊天秘密。那本书至今仍有一册残卷，流传于世的某个角落……',
    },
    noble: {
      title: '👑 前世权贵皇族',
      desc: '你前世生于高位，手握乾坤，见识过权力的顶端也尝过人心叵测的滋味。你深知一个眼神可以改变他人命运，也明白高处不胜寒的孤独。今生的你天生有气场，善于洞察人性，有时显得难以接近——那是曾经的王者印记。',
      hidden: '【隐藏揭秘】前世最辉煌时，你曾悄悄放走了一个理应被诛九族的人，只因那人说了一句让你颤动的话。那个人后来做了什么……改变了整个历史走向。',
    },
  }
}
```

---

## 8. 测试索引完整数据

### `data/tests.js`

```js
export const TESTS = [
  {
    id: 'past-life',
    title: '你的前世是什么身份？',
    category: 'destiny',
    categoryLabel: '命运',
    cover: '/images/covers/past-life.png',
    count: 23841,
  },
  {
    id: 'mental-age',
    title: '你真实的心理年龄是多少？',
    category: 'psychology',
    categoryLabel: '性格',
    cover: '/images/covers/mental-age.png',
    count: 31205,
  },
  {
    id: 'worker-type',
    title: '你是哪种打工人？',
    category: 'comedy',
    categoryLabel: '沙雕',
    cover: '/images/covers/worker-type.png',
    count: 18763,
  },
  {
    id: 'parallel-life',
    title: '你的平行宇宙人生是？',
    category: 'life',
    categoryLabel: '人生',
    cover: '/images/covers/parallel-life.png',
    count: 9547,
  },
  {
    id: 'deepest-fear',
    title: '你潜意识里最害怕什么？',
    category: 'psychology',
    categoryLabel: '性格',
    cover: '/images/covers/deepest-fear.png',
    count: 14392,
  },
]
```

---

## 9. 广告配置

| 广告位 | 广告类型 | 位置 | 触发方式 | 开发期 adUnitId |
|--------|----------|------|----------|----------------|
| 结果解锁 | 激励视频 | 结果解锁页 | 用户主动点击 | `adunit-test` |
| 隐藏内容 | 激励视频 | 结果页 | 用户主动点击 | `adunit-test` |
| 首页底部 | Banner | 首页底部固定 | 自动展示 | `adunit-test` |
| 结果页底部 | Banner | 结果页底部 | 自动展示 | `adunit-test` |

**开通条件**：上线后累计 1000 UV，在微信公众平台申请流量主资格，然后在 `app.js` 替换真实 `adUnitId`。

---

## 10. 技术栈

- **框架**：原生微信小程序（WXML / WXSS / JS），无第三方框架
- **广告**：`wx.createRewardedVideoAd` + `<ad>` 组件
- **分享海报**：Canvas 2D API（`type="2d"` 新版接口）
- **无后端**：全部内容本地硬编码，`require()` 动态加载测试数据

### 完整项目目录

```
miniprogram/
├── app.js
├── app.json
├── app.wxss
├── pages/
│   ├── index/
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   └── index.js
│   ├── quiz/
│   │   ├── quiz.wxml
│   │   ├── quiz.wxss
│   │   └── quiz.js
│   └── result/
│       ├── result.wxml
│       ├── result.wxss
│       └── result.js
├── data/
│   ├── tests.js
│   └── tests/
│       ├── past-life.js
│       ├── mental-age.js
│       ├── worker-type.js
│       ├── parallel-life.js
│       └── deepest-fear.js
├── utils/
│   ├── scoring.js
│   ├── ad.js
│   └── poster.js
└── images/
    ├── covers/
    │   ├── past-life.png
    │   ├── mental-age.png
    │   ├── worker-type.png
    │   ├── parallel-life.png
    │   └── deepest-fear.png
    └── qrcode.png
```

---

## 11. 上线路径

1. 注册微信小程序账号（服务类目：**娱乐 > 其他娱乐**）
2. 微信开发者工具新建项目，模板选择 **JavaScript - 基础模板**（不选 TypeScript、不选云开发、不选"不使用模板"）
3. 删除自动生成的示例页面（如 `pages/logs`），按本文档三页结构（`index`、`quiz`、`result`）重建
4. 本地真机调试，验证广告测试 ID 是否正常展示
5. 上传代码 → 提交审核（通常 1~3 个工作日）
6. 上线后推广，积累到 **1000 UV**
7. 申请开通流量主，替换 `app.js` 中广告单元 ID 为真实 ID
8. 持续用 AI 生成新测试内容，每周上新 1~2 套

---

## 12. 方案评审与已知问题

以下是对当前方案的评审，开发时需注意修正。

### 12.1 结果页"全遮"策略需优化

当前设计：用户答完题后结果完全被模糊遮罩覆盖，必须看激励视频才能看到任何内容。这会导致跳出率过高。

**建议改为"半遮半露"策略：**
- 免费展示结果标题 + 1~2 句概括（如让用户知道自己是"前世高僧"还是"沙场猛将"）
- 激励视频解锁详细解读 + 隐藏内容
- 用户看到标题后有分享冲动，同时好奇心驱动看视频解锁详情
- 对广告收入几乎无负面影响，但能大幅降低结果页跳出率

### 12.2 广告监听器重复注册 Bug

结果页 `onUnlockTap` 和 `onUnlockHiddenTap` 每次点击都会调用 `.onClose()` 注册新监听器，导致回调累积。

**修正方案：** 在 `onLoad` 中一次性注册 `onClose`，通过状态变量（如 `this._adAction = 'unlock' | 'hidden'`）区分解锁类型，或直接使用 `utils/ad.js` 中已封装好的 `showRewardedAd` 函数。

### 12.3 `require()` 动态路径不支持

微信小程序的 `require` 只接受**静态字符串路径**，不支持模板字符串拼接：

```js
// 不支持 ——
const testModule = require(`../../data/tests/${testId}`)

// 需要改为映射表 ——
const TEST_MAP = {
  'past-life':     require('../../data/tests/past-life'),
  'mental-age':    require('../../data/tests/mental-age'),
  'worker-type':   require('../../data/tests/worker-type'),
  'parallel-life': require('../../data/tests/parallel-life'),
  'deepest-fear':  require('../../data/tests/deepest-fear'),
}
const test = TEST_MAP[testId].default
```

### 12.4 `switchTab` 无 `tabBar` 配置

`result.wxml` 中 `<navigator open-type="switchTab">` 会报错，因为 `app.json` 未配置 `tabBar`。应改为 `open-type="reLaunch"` 或 `open-type="redirect"`。

### 12.5 `ctx.roundRect()` 兼容性

`poster.js` 中使用的 `ctx.roundRect()` 是较新的 Canvas API，低版本基础库不支持。需要做兼容处理或手动绘制圆角矩形（`moveTo` + `arcTo`）。

### 12.6 数据冗余

`data/tests.js`（索引）和各 `data/tests/*.js`（详情）之间 id、title、category 字段重复存储。维护多套测试时容易不一致。建议索引文件只存 id 和展示信息（cover、count），详情文件作为唯一数据源。

---

## 13. 主题扩展规划

当前仅有 5 套测试、4 个分类，内容量不足以支撑用户留存。建议上线前扩展到 **6~7 个分类、15~20 套测试**。

### 13.1 新增分类：「关系」（relationship）

标签色：`#e05297`（粉紫）

| 测试标题 | 卖点 |
|---------|------|
| 你在关系里是哪种依恋类型？ | 依恋理论有学术背书，用户认知度高，结果容易引发讨论 |
| 你的友情保质期还剩多久？ | 扎心向，朋友间互测传播力强 |
| 你最容易被哪种人格吸引？ | MBTI 热度嫁接，天然有社交话题 |

### 13.2 新增分类：「脑洞」（brainhole）

标签色：`#22c55e`（绿）

| 测试标题 | 卖点 |
|---------|------|
| 你在末日废土里能活几天？ | 生存模拟，年轻人喜欢的题材 |
| 如果你穿越到古代，会怎么死？ | 反套路，"怎么死"比"成为谁"更有传播欲 |
| AI 觉得你像哪个互联网梗？ | 蹭 AI 热点 + 梗文化，结果天然适合截图分享 |
| 你的灵魂是哪种食物？ | 无厘头向，结果（如"你是一碗螺蛳粉"）有强烈分享冲动 |

### 13.3 现有分类内容升级

**命运类——降低玄学感，增加故事性：**

| 状态 | 标题 | 改进点 |
|------|------|--------|
| 保留 | 你的前世是什么身份？ | 经典款，保留 |
| 新增 | 你命中注定会错过什么？ | 遗憾向叙事，情绪共鸣强 |
| 新增 | 你的平行宇宙在过什么人生？ | 替代现有"平行宇宙"，用更具体的叙事 |

**性格类——往"精准扎心"方向走：**

| 标题 | 卖点 |
|------|------|
| 你嘴上说不要，身体最诚实的欲望是？ | 标题有张力，结果可以做得很有洞察感 |
| 你在朋友眼里的"最大缺点"是什么？ | 反向测试，戳中用户想知道又怕知道的心理 |
| 你最擅长自我欺骗的是哪件事？ | 深度向，适合成年用户 |

**沙雕类——加重"玩梗"属性：**

| 标题 | 卖点 |
|------|------|
| 你是领导眼中的哪种员工？ | 职场梗，打工人共鸣 |
| 你退休后会变成哪种大爷/大妈？ | 反差萌，年轻人想象自己老了的样子 |
| 你的精神状态对应哪个表情包？ | 结果直接就是表情包图，截图传播率极高 |

### 13.4 季节性 / 热点内容

长期运营关键差异化手段，按时间节点上新：

- **毕业季**："你的大学四年值多少钱？"
- **春节**："你在亲戚眼中是什么人设？"
- **高考季**："你的灵魂学历是什么？"
- **年末**："你的年度精神状态报告"

### 13.5 配色方案补充

新增分类需要在全局配色中补充对应颜色：

```css
/* 新增分类标签色 */
--color-relationship: #e05297;   /* 关系 */
--color-brainhole:    #22c55e;   /* 脑洞 */
```

海报渐变色补充：

| 分类 | 渐变起止色 |
|------|-----------|
| relationship（关系） | `#2a0a1a` → `#5a1a3a` |
| brainhole（脑洞） | `#0a2a0a` → `#1a5a1a` |

分类 Tab 数据需同步更新：

```js
categories: [
  { key: 'all',          label: '全部' },
  { key: 'destiny',      label: '命运' },
  { key: 'psychology',   label: '性格' },
  { key: 'relationship', label: '关系' },
  { key: 'life',         label: '人生' },
  { key: 'brainhole',    label: '脑洞' },
  { key: 'comedy',       label: '沙雕' },
]
```

---

## 14. 未来迭代方向（v2）

- **后端 CMS**：运营后台随时上新测试，无需发版
- **分支叙事**：答案影响后续题目走向，提升沉浸感
- **排行榜**：「本周最多人测的结果」，制造社交话题
- **每日一测**：签到机制，提升次日留存
- **微信好友 PK**：分享结果后，好友可来测同一套并对比结果
